extends layout

block content
    
  .w3-card-4
    header.w3-container.w3-teal
      h1
        span Author #{autor}

    
    p Número de livros: #{somatorio}

    .w3-container
      table.w3-table-all
        tr
          th id
          th title
          th publishDate
          th pages
          
        each livro in livros
          tr
            td
                a(href="/" + livro._id)= livro._id
            td= livro.title
            td= livro.publishDate
            td= livro.pages

    footer.w3-container.w3-teal
            a(href="/") Voltar